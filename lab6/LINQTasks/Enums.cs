namespace LINQTasks {
    public enum UserType {
        Manager,
        Developer,
        Tester,
    }

    public enum Status {
        Created, 
        Fixed,
        Closed,
    }

    public enum Priority {
        Minor,
        Normal,
        Critilal,
    }
}